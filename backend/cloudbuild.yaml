steps:
  - name: 'gcr.io/kaniko-project/executor:latest'
    args:
      - '--destination=${_REGISTRY}/paygles-api:latest'
      - '--cache=true'
      - '--cache-ttl=168h'
options:
  logging: CLOUD_LOGGING_ONLY
