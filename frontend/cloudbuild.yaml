steps:
  - name: 'gcr.io/kaniko-project/executor:latest'
    args:
      - '--destination=${_REGISTRY}/paygles-web:latest'
      - '--cache=true'
      - '--cache-ttl=168h'
      - '--build-arg=VITE_API_URL=${_VITE_API_URL}'
options:
  logging: CLOUD_LOGGING_ONLY
